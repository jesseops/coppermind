services:
  coppermind:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./data:/data
    environment:
      - COPPERMIND_DB=/data/coppermind.db
      - COPPERMIND_DATA_DIR=/data
      - COPPERMIND_HOST=0.0.0.0
      - COPPERMIND_PORT=5000
    restart: unless-stopped

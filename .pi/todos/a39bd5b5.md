{
  "id": "a39bd5b5",
  "title": "Phase 3: Small polish — sort controls, CSP, health endpoint, warnings",
  "tags": [
    "polish",
    "nice-to-have",
    "phase-3"
  ],
  "status": "done",
  "created_at": "2026-04-27T15:10:48.606Z"
}

## Items (one commit)
1. **Sort controls** — Add sort dropdown (title, author, date added, year) to home page
   next to the type filter. Wire up via HTMX like the existing type filter.
2. **Session secret warning** — Log `slog.Warn` at startup when secret is auto-generated
3. **Content-Security-Policy header** — Add CSP in `securityHeaders` middleware:
   `default-src 'self'; style-src 'self' 'unsafe-inline' fonts.googleapis.com; font-src fonts.gstatic.com; img-src 'self' covers.openlibrary.org data:; script-src 'self' 'unsafe-inline'`
4. **Health endpoint** — `GET /health` → 200 + DB ping
5. **setupCheckMiddleware caching** — Replace per-request `CountUsers()` with a
   `sync.Once` that flips after first user is created

## Open question
→ **For CSP: `'unsafe-inline'` is needed for the theme init script and inline
  styles. Acceptable?** Alternative is nonce-based CSP which is more complex.
  Given this is a personal/small-team app, `unsafe-inline` for scripts is
  pragmatic but technically weakens XSS protection.

## Scope
- `server.go`, `handlers.go`, `home.html`
- ~50 lines total
